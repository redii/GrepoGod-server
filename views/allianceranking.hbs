<!DOCTYPE html>
<html lang="en">

  {{> head}}

  <body>

    {{> navbar}}

    <div class="container">
      <div class="row mt-5"></div>
      <div class="row">
        <div class="col-md-12 text-center">
          <h1>Alliance Ranking for {{world}}</h1>
          <p class="text-muted" style="font-size: 13px">You can go back to your dashboard by clicking <a href="/dashboard/{{world}}">here</a>. Use Ctrl+F to search for players.</p>
        </div>
      </div>
    </div>
    <div class="container-fluid">
      <div class="row mt-3">
        <div class="col-md-3"></div>
        <div class="col-md-6">
          <table class="table table-sm table-bordered table-hover">
            <thead>
              <tr>
                <th class="text-center" scope="col">Rank</th>
                <th scope="col">Name</th>
                <th class="text-center" scope="col">Points</th>
                <th class="text-center" scope="col"># Members</th>
                <th class="text-center" scope="col"># Towns</th>
                <th class="text-center" scope="col">Bash All</th>
                <th class="text-center" scope="col">Bash Off</th>
                <th class="text-center" scope="col">Bash Def</th>
              </tr>
            </thead>
            <tbody>
              {{#each alliance}}
              <tr>
                <th class="text-center" scope="row">{{rank}}</th>
                <td><a href="/alliancesearch/{{../world}}/{{name}}">{{name}}</a></td>
                <td class="text-center" data-toggle="tooltip" data-placement="bottom" title="{{number points}}">{{number points "0.0a"}}</td>
                <td class="text-center">{{members}}</td>
                <td class="text-center">{{towns}}</td>
                <td class="text-center" data-toggle="tooltip" data-placement="bottom" title="{{number killsall}}">{{number killsall "0.0a"}}</td>
                <td class="text-center" data-toggle="tooltip" data-placement="bottom" title="{{number killsoff}}">{{number killsoff "0.0a"}}</td>
                <td class="text-center" data-toggle="tooltip" data-placement="bottom" title="{{number killsdef}}">{{number killsdef "0.0a"}}</td>
              </tr>
              {{/each}}
            </tbody>
          </table>
        </div>
        <div class="col-md-3"></div>
      </div>
    </div>

    {{> js}}

  </body>
</html>
